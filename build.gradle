plugins {
    id "org.sonarqube" version "4.4.1.3373" // or latest
}

sonarqube {
    properties {
        property "sonar.projectKey", "monikabole_devops-practice"
        property "sonar.organization", "monikabole"
        property "sonar.host.url", "https://sonarcloud.io"
    }
}
